import {
  getFilms,
  getPeople,
  getPlanets,
  getVehicles,
  fetchHomeworld,
  fetchSpecies,
  cleanData,
  fetchResidents
} from './utility.js';

describe('getFilms', () => {
  it('should be a function', () => {
    expect(getFilms).toBeAFunction;
  });
});

describe('getPeople', () => {
  it('should be a function', () => {
    expect(getPeople).toBeAFunction;
  });
});

describe('getPlanets', () => {
  it('should be a function', () => {
    expect(getPlanets).toBeAFunction;
  });
});

describe('getVehicles', () => {
  it('should be a function', () => {
    expect(getVehicles).toBeAFunction;
  });
});
describe('fetchHomeworld', () => {
  it('should be a function', () => {
    expect(fetchHomeworld).toBeAFunction;
  });
});
describe('fetchSpecies', () => {
  it('should be a function', () => {
    expect(fetchSpecies).toBeAFunction;
  });
});
describe('cleanData', () => {
  it('should be a function', () => {
    expect(cleanData).toBeAFunction;
  });
});
describe('fetchResidents', () => {
  it('should be a function', () => {
    expect(fetchResidents).toBeAFunction;
  });
});
